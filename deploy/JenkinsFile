pipeline {
    agent any
    stages{
        stage("listar directorio actual"){
            steps {
                sh "ls -la"
            }
        }
        stage("Build the docker"){
            steps{
                sh "cd deploy"
                sh "docker build -t web:latest ."
            }
        }
    }
}